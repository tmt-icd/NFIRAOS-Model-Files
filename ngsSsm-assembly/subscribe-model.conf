subsystem = NFIRAOS
component = ngsSsm

subscribe {
  events = [
    {
      subsystem     = TCS
      component     = cmNFIRAOS
      name          = ngsPos
      requiredRate  = 20
    }
    {
      subsystem     = NFIRAOS
      component     = ngsAdc 
      name          = imageShift
      requiredRate  = 20
    }
    {
      subsystem     = NFIRAOS
      component     = ngsAdc 
      name          = pupilShift
      requiredRate  = 20
    }
    {
      subsystem     = NFIRAOS
      component     = rtc 
      name          = pwfsSsm
    }
  ]
}
