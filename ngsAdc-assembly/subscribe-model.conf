subsystem = NFIRAOS
component = ngsAdc

subscribe {
  events = [
    {
      subsystem     = TCS
      component     = cmNFIRAOS 
      name          = pwfsAtmDispersion
      requiredRate  = 1
    }
  ]
}
