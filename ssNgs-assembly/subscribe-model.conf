subsystem = NFIRAOS
component = ssNgs

subscribe {
  events = [
    {
      subsystem     = NFIRAOS
      component     = safe
      name          = galilMotorStatus
      usage         = """
The SS NGS monitors the NFIRAOS Safety System for any interlocks that may affect its motors.
"""
    }
  ]
}