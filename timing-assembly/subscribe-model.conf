subsystem = NFIRAOS
component = timing

subscribe {
  telemetry = [
    {
      subsystem     = AOESW
      component     = RPG
      name          = nrtcTriggerPulse
      requiredRate  = 3.33
    }
  ]
}
